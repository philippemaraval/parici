{
  "name": "camino-render-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Render compatibility launcher; dependencies remain declared at the repository root.",
  "scripts": {
    "postinstall": "npm ci --prefix .. --include=dev --ignore-scripts && npm run build --prefix ..",
    "start": "node server.js"
  },
  "engines": {
    "node": ">=22 <23"
  }
}
